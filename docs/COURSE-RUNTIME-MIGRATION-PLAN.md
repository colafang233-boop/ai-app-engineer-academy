# Course Runtime Migration Plan

- Preserve the existing V3 application while the runtime is introduced.
- Build browser-native ES modules with no build step.
- Migrate Lesson 3 first as the reference implementation.
- Keep the V3 ArtifactStore key compatible during migration.
